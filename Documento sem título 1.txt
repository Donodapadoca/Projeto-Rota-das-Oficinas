<!DOCTYPE html>
<html>
<head>
  <title>Calculadora de Conta</title>
  
  body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 20px;
}

h1, h2 {
  text-align: center;
}

#input-form {
  margin-bottom: 20px;
}

#items {
  margin-bottom: 10px;
}

.item {
  display: flex;
  margin-bottom: 5px;
}

.item input {
  margin-right: 5px;
}

#output {
  text-align: center;
}

#results {
  list-style-type: none;
  padding: 0;
}

#results li {
  margin-bottom: 5px;
}

</head>
<body>
  <h1>Calculadora de Conta</h1>

  <div id="input-form">
    <label for="total-bill">Valor Total da Conta:</label>
    <input type="number" id="total-bill" step="0.01" min="0">

    <div id="items">
      <div class="item">
        <input type="text" class="product" placeholder="Produto">
        <input type="number" class="price" step="0.01" min="0" placeholder="Preço">
        <select class="consumed-by">
          <option value="">Selecione um cliente</option>
        </select>
      </div>
    </div>

    <button id="add-item">Adicionar Produto</button>

    <button id="calculate" disabled>Calcular</button>
  </div>

  <div id="output">
    <h2>Divisão da Conta</h2>
    <ul id="results"></ul>
  </div>
	<script>
  document.addEventListener('DOMContentLoaded', function() {
  const addItemButton = document.getElementById('add-item');
  const calculateButton = document.getElementById('calculate');
  const resultsList = document.getElementById('results');

  addItemButton.addEventListener('click', addItem);
  calculateButton.addEventListener('click', calculate);

  function addItem() {
    const itemsContainer = document.getElementById('items');

    const itemDiv = document.createElement('div');
    itemDiv.className = 'item';

    const productInput = document.createElement('input');
    productInput.type = 'text';
    productInput.className = 'product';
    productInput.placeholder = 'Produto';

    const priceInput = document.createElement('input');
    priceInput.type = 'number';
    priceInput.step = '0.01';
    priceInput.min = '0';
    priceInput.className = 'price';
    priceInput.placeholder = 'Preço';

    const consumedBySelect = document.createElement('select');
    consumedBySelect.className = 'consumed-by';

    const defaultOption = document.createElement('option');
    defaultOption.value = '';
    defaultOption.text = 'Selecione um cliente';
    defaultOption.disabled = true;
    defaultOption.selected = true;

    consumedBySelect.appendChild(defaultOption);

    itemDiv.appendChild(productInput);
    itemDiv.appendChild(priceInput);
    itemDiv.appendChild(consumedBySelect);

    itemsContainer.appendChild(itemDiv);
  }

  function calculate() {
    const totalBillInput
    </script>

</body>
</html>






